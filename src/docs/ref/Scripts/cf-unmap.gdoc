h1. cf-unmap

h2. Purpose

Unregisters a URL from an application.

h2. Description

Unregisters a URL from the specified application.

Usage:
{code:java}
grails cf-unmap <url> [--appname]
{code}

Required arguments:

* @url@ - The url, e.g. @yoururl.cloudfoundry.com@

Optional arguments:

* @appname@ - The name of the application
